# URL of your Home Assistant instance, reachable from wherever this app runs.
# Use the local network address of your mini PC, e.g.:
HA_URL=http://homeassistant.local:8123

# Long-lived access token, created in HA under:
# Profile (click your name, bottom left) -> Security -> Long-Lived Access Tokens -> Create Token
HA_TOKEN=
